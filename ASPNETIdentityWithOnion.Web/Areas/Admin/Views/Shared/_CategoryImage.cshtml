@model ASPNETIdentityWithOnion.Core.DomainModels.StoreModels.Category
@*<link href="~/Content/bootstrap.css" rel="stylesheet" />
@using (Html.BeginForm("Upload", "Category", Model, FormMethod.Post, new { enctype = "multipart/form-data" }))
{
    <div class="form-group">
        <div class="col-md-10">
            <label for="file" class="control-label col-md-2">Image</label>
            <a class='btn' href='javascript:;'>
        Choose File...
                <input type="file" name="uploadfile" size="220"
                       style="position:absolute;z-index:2;top:0;
                         left:0;filter: alpha(opacity=0); opacity:0;
                         background-color:transparent;color:transparent;"
                       multiple accept="image/*,image/jpeg"
               onchange='$("#upload-file-info").html($(this).val());'>
    </a>
    <span class='label label-info' id="upload-file-info"></span>
            <input class="control-label" type="file" name="uploadfile" id="file" />
        </div>
        @if (Model.Path != null)
        {
            <img class="img-thumbnail" width="150" height="150"
                 src="@Url.Content(@Model.Path)" />
        }
        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Load" class="btn btn-default" />
            </div>
        </div>
        @if (ViewBag.err != null)
        {
            <div class="form-control-static" style="color:red">@ViewBag.err</div>


        }
        
    </div>

}*@
<div class="container">
    <div id="linkupload">Load file</div>
    <img src="" />
    <input type="file" name="upload" />
    <div class="text">File is not upload</div>
</div>

<!---->
<!---->
<!---->